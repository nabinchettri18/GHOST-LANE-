'use client'
import { FormEvent, useState } from 'react'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/client'
export default function SignupPage(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setLoading(true);const f=new FormData(e.currentTarget);const email=String(f.get('email')||'').trim();const password=String(f.get('password')||'');const supabase=createClient();const {error}=await supabase.auth.signUp({email,password});if(error)setError(error.message);else window.location.assign('/login?registered=1');setLoading(false)}return <main className="min-h-screen bg-[#f7f9fc] px-5 py-10 text-[#08111f]"><div className="mx-auto max-w-md"><Link href="/login" className="text-sm font-bold text-[#1769e0]">← Back to sign in</Link><div className="mt-8 rounded-2xl border border-[#dbe2ec] bg-white p-8 shadow-[0_12px_40px_rgba(8,17,31,.05)]"><div className="mb-8"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#1769e0] font-extrabold text-white">G</div><span className="text-lg font-extrabold">GhostLane</span></div><h1 className="mt-8 text-2xl font-extrabold tracking-tight">Create your workspace</h1><p className="mt-2 text-sm text-[#66758a]">Start with your own operational data.</p></div><form onSubmit={submit} className="space-y-5"><div><label className="mb-2 block text-sm font-bold">Work email</label><input name="email" type="email" required autoComplete="email" className="h-12 w-full rounded-xl border border-[#cbd6e5] px-4 text-sm outline-none focus:border-[#1769e0] focus:ring-4 focus:ring-[#1769e0]/10" placeholder="name@company.com"/></div><div><label className="mb-2 block text-sm font-bold">Password</label><input name="password" type="password" required minLength={8} autoComplete="new-password" className="h-12 w-full rounded-xl border border-[#cbd6e5] px-4 text-sm outline-none focus:border-[#1769e0] focus:ring-4 focus:ring-[#1769e0]/10" placeholder="At least 8 characters"/></div>{error&&<p role="alert" className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="h-12 w-full rounded-xl bg-[#1769e0] text-sm font-bold text-white hover:bg-[#0f57bd] disabled:opacity-60">{loading?'Creating…':'Create account'}</button></form><p className="mt-6 text-center text-xs text-[#8290a2]">You'll receive an email if verification is enabled for this workspace.</p></div></div></main>}
