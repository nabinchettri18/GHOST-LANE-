'use client'
import { FormEvent, useState } from 'react'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/client'
export default function ForgotPasswordPage(){const [message,setMessage]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage('');setError('');setLoading(true);const email=String(new FormData(e.currentTarget).get('email')||'').trim();const supabase=createClient();const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/reset`});if(error)setError(error.message);else setMessage('If an account exists for that email, a password reset link has been sent.');setLoading(false)}return <main className="min-h-screen bg-[#f7f9fc] px-5 py-10 text-[#08111f]"><div className="mx-auto max-w-md"><Link href="/login" className="text-sm font-bold text-[#1769e0]">← Back to sign in</Link><div className="mt-8 rounded-2xl border border-[#dbe2ec] bg-white p-8 shadow-[0_12px_40px_rgba(8,17,31,.05)]"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#edf4ff] text-[#1769e0] font-black">G</div><h1 className="mt-6 text-2xl font-extrabold">Reset your password</h1><p className="mt-2 text-sm leading-6 text-[#66758a]">Enter your work email and we'll send instructions if the account exists.</p><form onSubmit={submit} className="mt-7 space-y-5"><div><label className="mb-2 block text-sm font-bold">Work email</label><input name="email" type="email" required autoComplete="email" className="h-12 w-full rounded-xl border border-[#cbd6e5] px-4 text-sm outline-none focus:border-[#1769e0] focus:ring-4 focus:ring-[#1769e0]/10" placeholder="name@company.com"/></div>{error&&<p role="alert" className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}{message&&<p className="rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-700">{message}</p>}<button disabled={loading} className="h-12 w-full rounded-xl bg-[#1769e0] text-sm font-bold text-white hover:bg-[#0f57bd] disabled:opacity-60">{loading?'Sending…':'Send reset link'}</button></form></div></div></main>}
